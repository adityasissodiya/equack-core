# === Rust / Cargo ===
/target/
**/target/
**/*.rs.bk

# Keep Cargo.lock (reproducibility). Do NOT ignore it.

# === Editor / OS cruft ===
*.swp
*.swo
*~
.DS_Store
Thumbs.db
.idea/
.vscode/
.direnv/

# === Environment / local config ===
.env
.env.*
*.local

# === Logs / temp ===
*.log
*.tmp
tmp/
.cache/

# === Nix / Docker (build artifacts) ===
result
dist/
*.tar
*.tar.gz

# === Node (if ui/ appears later) ===
node_modules/
**/node_modules/

# === Python virtual environments ===
.venv/
venv/

# === LaTeX build artifacts ===
*.aux
*.out
*.synctex.gz

# === Backup files ===
*.bak

# === Local database state ===
.m8test.db/
**/.ecac.db/

# === Project artifacts we don't commit (paper run outputs) ===
docs/eval/out/
!docs/eval/golden/**

# === Security / release extras (generated) ===
sbom.json
cosign.sig
SHA256SUMS
