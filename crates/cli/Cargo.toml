[package]
name = "ecac-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_cbor = "0.11"
ecac-core = { path = "../core" }
blake3 = "1"

[dev-dependencies]
rand = "0.8"